# Database Configuration (if using external database)
# DATABASE_URL=your_database_url_here

# Node Environment
NODE_ENV=production

# Server Port (Not used in Vercel, but kept for local development)
PORT=5000

# Add any other environment variables your app needs
# For example, if you have external API keys, authentication secrets, etc.